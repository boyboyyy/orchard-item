<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.orchard.mapper.HarvestRecordMapper">

    <select id="orcharArea" resultType="com.orchard.entity.HarvestRecord">
        select *
        from harvest_record
        order by create_time desc
    </select>

</mapper>